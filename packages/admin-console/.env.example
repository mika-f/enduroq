# URL of the Enduroq queue server (no trailing slash needed)
ENDUROQ_SERVER_URL=http://127.0.0.1:7225

# Bearer token, only required if the queue server runs with ENDUROQ_AUTH_TOKEN set.
# Kept server-side; never exposed to the browser.
# ENDUROQ_AUTH_TOKEN=your-secret-token
